# One Hades suite folder name per line, relative to tests/.
# driver.bat test          - runs every suite listed here
# driver.bat test -ls      - lists every suite and its test ids without running anything

KerbecsSuite
CoriumSuite